
import CustomizeDrawingButton from './CustomizeDrawingButton';

let BlueDrawingButton = CustomizeDrawingButton.extend({
  name :'blueDrawing',
  color:'rgba(0, 0, 255, 1)'
});

export default BlueDrawingButton;

